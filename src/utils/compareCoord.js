// // src/utils/compareCoordinates.js
// export const areCoordinatesEqual = (coords1, coords2, tolerance = 0.000001) => {
//     if (coords1.length !== 2 || coords2.length !== 2) {
//       return false;
//     }
//     const [lat1, lon1] = coords1;
//     const [lat2, lon2] = coords2;
  
//     const latEqual = Math.abs(lat1 - lat2) < tolerance;
//     const lonEqual = Math.abs(lon1 - lon2) < tolerance;
  
//     return latEqual && lonEqual;
//   };
  